const { a } = require('../src/index')

describe('a', () => {
    it('test', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
    })
    it('test2', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
    })
})