const { a } = require('../src/index')

describe('a', () => {
    it('test', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
        expect(a()).toBeUndefined()
    })
    it('test1', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
        expect(a()).toBeUndefined()
    })
    it('test2', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
        expect(a()).toBeUndefined()
    })
    it('test3', () => {
        // is function
        expect(a).toBeInstanceOf(Function)
        expect(a()).toBeUndefined()
    })
})